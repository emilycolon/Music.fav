<%- include('../partials/header') %>

    <h1>TESTING SONG SHOW</h1>

    <ul>
        <% songs.forEach((song)=> { %>
            <li>
                "<%= song.title %>" <a href="/song/edit/<%= song.id %>">EDIT BUTTON</a> <a href="#">DELETE BUTTON</a>
            </li>
            <% }); %>
    </ul>

    <%- include('../partials/footer') %>