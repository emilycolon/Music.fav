<%- include('../partials/header')%>

    <h1>TESTING ARTIST SHOW</h1>

    <ul>
        <% artists.forEach((artist)=> { %>
            <li>
                <%= artist.name %> <a href="/artist/edit/<%= artist.id %>">EDIT BUTTON</a> <form action="/artist/<%= artist.id %>?_method=delete" method="post">
                <input type="submit" value="Remove">
              </form>
            </li>
            <% }); %>
    </ul>

    <%- include('../partials/footer')%>