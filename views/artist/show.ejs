<%- include('../partials/header')%>
    <div class="flex">
        <div class="content-container">
            <div class="title">
                <h1>Favorited Artists</h1>
            </div>
            <div class="content">
                <ul>
                    <% artists.forEach((artist)=> { %>
                        <li>
                            <%= artist.name %>
                                <a href="/artist/edit/<%= artist.id %>" class="edit">
                                    <i class="far fa-edit"></i>
                                </a>
                                <form method="POST" action="/artist/<%= artist.id %>?_method=delete" style="display: inline;">
                                    <button type="submit" name="_method" value="DELETE" class="circle">
                                        <i class="far fa-times-circle"></i>
                                    </button>
                                </form>
                        </li>
                        <% }); %>
                </ul>
            </div>
        </div>
    </div>
    <%- include('../partials/footer')%>