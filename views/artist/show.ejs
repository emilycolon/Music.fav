<%- include('../partials/header') %>

    <h1>TESTING ARTIST SHOW</h1>

    <ul>
        <% artists.forEach((artist)=> { %>
            <li>
                <%= artist.name %> <a href="/artist/edit/<%= artist.id %>">EDIT BUTTON</a> <a href="#">DELETE BUTTON</a>
            </li>
            <% }); %>
    </ul>

    <%- include('../partials/footer') %>